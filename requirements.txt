# Python is installed separately; this project selects Python 3.12 in
# `.python-version` for the stable vLLM/CUDA environment.

# Core data processing and local IO.
numpy>=2.0,<2.5
pyarrow>=15.0.0
py7zr>=0.21.0
httpx>=0.27.0
jsonschema>=4.23.0
PyYAML>=6.0.1

# Lab/GPU serving dependency for the model-specific `vllm_server_*.yaml` profiles.
# vLLM is Linux/CUDA-oriented; this marker avoids installing it on native Windows.
# vLLM pins compatible PyTorch and CUDA runtime wheels transitively. Do not add
# individual `nvidia-*` packages here or mix CUDA 12 and CUDA 13 wheel families.
transformers>=5.8.0,<6.0.0; platform_system == "Linux"
vllm>=0.25.1,<0.26.0; platform_system == "Linux"
