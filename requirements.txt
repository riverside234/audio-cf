# Python >= 3.10 recommended. Use Python 3.12 on the lab GPU machine if possible.

# Core data processing and local IO.
pyarrow>=15.0.0
py7zr>=0.21.0
httpx>=0.27.0
jsonschema>=4.21.0
PyYAML>=6.0.1

# Optional graph wrapper used by synthetic.agents.graph.build_langgraph_app().
# The default data_synthetic.py path can run without full LangChain.
langgraph>=0.2.0

# Lab/GPU serving dependency for `vllm serve --config configs/vllm_server.yaml`.
# vLLM is Linux/CUDA-oriented; this marker avoids installing it on native Windows.
# Keep this recent for Gemma 4 reasoning parser + structured JSON outputs.
vllm>=0.11.0; platform_system == "Linux"
